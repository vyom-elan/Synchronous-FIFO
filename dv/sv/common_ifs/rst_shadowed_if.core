

name: "server_iddv:rst_shadowed_if"
description: "Shadowed reset pin interfaces used in DV"

filesets:
  files_dv:
    files:
      - rst_shadowed_if.sv
    file_type: systemVerilogSource

targets:
  default:
    filesets:
      - files_dv
