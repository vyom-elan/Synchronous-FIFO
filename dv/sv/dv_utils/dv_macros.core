CAPI=2:

name: "serverhost:dv:dv_macros"
description: "A collection of macros used in DV."

filesets:
  files_dv:
    files:
      - dv_macros.svh: {is_include_file: true}
    file_type: systemVerilogSource

targets:
  default: &default_target
    filesets:
      - files_dv
