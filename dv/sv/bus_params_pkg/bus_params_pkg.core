name: "serverid:opentitan:bus_params_pkg"
description: "Package holding common bus parameters such as widths."

filesets:
  files_dv:
    depend:
      - lowrisc:virtual_constants:top_pkg
    files:
      - bus_params_pkg.sv
    file_type: systemVerilogSource

targets:
  default:
    filesets:
      - files_dv
