CAPI=2:

name: "server_name:dv:fifo_sva:0.1"
description: "FIFO assertion modules and bind file."
filesets:
  files_dv:
    files:
      - fifo_bind.sv
    file_type: systemVerilogSource

  files_formal:
    depend:
      - lowrisc:ip:fifo


targets:
  default: &default_target
    filesets:
      - files_dv
